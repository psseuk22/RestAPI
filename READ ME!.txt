What You Need To Do:

1-Install python with the PythonInstaller in the folder.

2-Type "cmd" in the address bar and paste the codes below into the console that opens.

pip install --user discord
pip install --user colorama
pip install --user psutil
pip install --user pypresence

3-Close the CMD you opened and open it again in the same way.

4-Run the application.

> python main.py
